# LM Studio
path_add "$HOME/.cache/lm-studio/bin"
path_add "$HOME/.lmstudio/bin"

# opencode
path_add "$HOME/.opencode/bin"

# bob (nvim version management)
path_add "$HOME/.local/bin"
path_add "$HOME/.local/share/bob/nvim-bin"
